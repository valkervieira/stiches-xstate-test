import { H1 } from '@components/Text';
import { Layout } from '@templates/Layout';

export const DocsTemplate = (): JSX.Element => {
  return (
    <Layout>
      <H1>Docs page</H1>
    </Layout>
  );
};

import { H1 } from '@components/Text';
import { Layout } from '@templates/Layout';

export const PlansTemplate = (): JSX.Element => {
  return (
    <Layout>
      <H1>Plans page</H1>
    </Layout>
  );
};
